#footer
  .center
    -Static.all.each do |site|
      =link_to(site.title,static_path(site)) if permitted_to?(:show,site)
    =link_to("List of Moderators",mods_path)if permitted_to?(:index_mods,:users) 
