.center
  =render(:partial=>"layouts/sidebar")
  .with_sidebar
    -unless flash[:notice].nil?
      #status
        =boxed "Status" do
          =notice
          :javascript
            setTimeout("new Effect.DropOut('status');", 7000);
    =yield 
  =clear
